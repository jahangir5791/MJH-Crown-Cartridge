# Web Frontend

Browser-based version of MJH-Crown-Cartridge.

## Setup

```bash
npm install
npm run dev
Build
npm run build
Features
WebGL game rendering
PWA support (offline mode)
Multiplayer via WebSocket
Cloud save integration
